# Useful dotnet command line commands

`dotnet --help` shows top-level .NET CLI commands. A good place to start for looking for commands to run.
`dotnet new` - The 'dotnet new' command creates a .NET project based on a template. It gives some common examples you could choose.
`dotnet new list` shows all installed templates.
